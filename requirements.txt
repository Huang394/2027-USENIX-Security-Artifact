# Artifact audit scripts under scripts/ use only the Python standard library.
# The packages below are needed for full main-experiment reruns,
# mechanism-analysis reruns, and developer validation.
#
# Tested environment: Python 3.9, torch 2.6.0, torchvision 0.21.0.
# Install a torch/torchvision build that matches the review machine's CUDA/CPU setup.

numpy==2.0.2
pandas==2.2.3
Pillow==11.1.0
PyYAML==6.0.2

torch==2.6.0
torchvision==0.21.0

opencv-python==4.11.0.86
imageio==2.37.0
tqdm==4.67.1
requests==2.32.3

matplotlib==3.9.4
seaborn==0.13.2
scikit-learn==1.6.1
scipy==1.13.1

einops==0.8.2

# Validation tools used by this artifact preparation.
ruff==0.15.15
mypy==1.19.1
